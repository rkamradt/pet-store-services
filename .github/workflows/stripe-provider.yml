name: "Build stripe-provider"

on:
  push:
    branches:
      - main
    paths:
      - "stripe-provider/**"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./stripe-provider
          push: true
          tags: ghcr.io/rkamradt/stripe-provider:main
